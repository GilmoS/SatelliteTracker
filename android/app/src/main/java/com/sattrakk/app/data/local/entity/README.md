# data/local/entity

Room `@Entity` classes go here (Satellite, TleRecord, Pass, Note, Settings —
mirroring the backend tables). None exist yet; wired up alongside
`di/DatabaseModule.kt` in Milestone E.
