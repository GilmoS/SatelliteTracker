# data/repository

Repository implementations combining the Retrofit API (`data/remote/`) and
Room DAOs (`data/local/`), exposed to `domain/` as plain Kotlin interfaces.
None exist yet — no ViewModels or real API calls in this skeleton.
