<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug-only (see app/src/debug/AndroidManifest.xml) — permits cleartext HTTP to the dev
    backend. BuildConfig.API_BASE_URL defaults to http://10.0.2.2:5076/ (see app/build.gradle.kts),
    and minSdk 29 blocks cleartext traffic app-wide by default, so without this every request
    against that default URL fails with "CLEARTEXT communication not permitted." Release builds
    use no networkSecurityConfig override and stay on the secure default (HTTPS only).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
